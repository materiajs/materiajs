import t from 'vue-types';

export default {
  props: {
    display: t.string,
    flex: t.string,
    flexDirection: t.string,
    gridTemplateColumns: t.string,
    margin: t.string,
    padding: t.string,
    width: t.string,
    maxWidth: t.string,
  },
};
